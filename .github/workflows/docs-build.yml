name: docs-build
on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]
  merge_group: ~
permissions:
  contents: read
  pull-requests: read
jobs:
  build:
    uses: elastic/docs-actions/.github/workflows/docs-build.yml@7227c43102bffcbd7303564886a58e7b6f46cfc5 # v1
    with:
      enable-vale-linting: true
