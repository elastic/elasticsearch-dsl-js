---
- job:
    name: elastic+elasticsearch-dsl-js+master
    display-name: 'elastic / elasticsearch-dsl-js # master'
    description: Testing the elasticsearch-dsl-js master branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/master
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
    triggers:
    - github
    - timed: '@daily'
